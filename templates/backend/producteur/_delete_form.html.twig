<form action="{{ url('app_admin.producer.delete', { id: visitor.id }) }}" method="post">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visitor.id) }}">
	<button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i></button>
</form>