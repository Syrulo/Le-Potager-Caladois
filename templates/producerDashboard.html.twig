{% extends './layout/base.html.twig' %}

{% block title %}
	Gestion de vos produits
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="content-header">
		<div class="container p-4">
			<div class="d-flex flex-column align-items-center">
				<h1 class="bienvenue">Bienvenue
					{{ app.user.firstname }}</h1>
				<h2>gestion de vos produits</h2>
				<div class="breadcrumb">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="{{ path('app_home_page') }}">Retour à l'accueil</a>
						</li>
						<li class="breadcrumb-item active">Gestion des produits</li>
					</ol>
				</div>
			</div>
		</div>
	</section>
	{% for message in app.flashes('error') %}
		<div class="alert alert-success" role="alert">
			{{ message }}
		</div>
	{% endfor %}
	<div class="d-flex justify-content-center">
		{% if visitor.status == 'pending' %}
			<div class="alert alert-danger" role="alert">
				Validation en cours
			</div>
		{% endif %}
	</div>
	{% if visitor.status == 'confirmed' and visitor.producers|length == 0 %}
		<section class="content">
			<div class="container-fluid p-5 mb-5">
				<div class="card col-lg-6 col-md-10 col-12 mx-auto p-3">
					<div class="card-header">
						<h3 class="mt4 text-center">Formulaire d'inscription</h3>
					</div>
					<div class="card-body">
						{{ form_start(form) }}
						<span class="text-danger">{{ form_errors(form.contactEmail) }}</span>
						{{ form_label(form.brandName, 'Nom commercial', {'label_attr': {'class': 'single-line-label'}}) }}
						<div class="input-group mb-3">
							{{ form_errors(form.brandName) }}
							{{ form_widget(form.brandName, {'attr': {'class': 'form-control', 'placeholder': 'Nom'}}) }}
							<div class="input-group-append">
								<span class="input-group-text">
									<span class="fas fa-user icon-large"></span>
								</span>
							</div>
						</div>
						{{ form_label(form.contactEmail) }}
						<div class="input-group mb-3">
							{{ form_widget(form.contactEmail, {'attr': {'class': 'form-control', 'placeholder': 'Email'}}) }}
							<div class="input-group-append">
								<span class="input-group-text">
									<span class="fas fa-envelope icon-large"></span>
								</span>
							</div>
						</div>
						{{ form_label(form.phone, null, {'label_attr': {'class': 'single-line-label'}}) }}
						<div class="input-group mb-3">
							{{ form_errors(form.phone) }}
							{{ form_widget(form.phone, {'attr': {'class': 'form-control', 'placeholder': 'Numéro de téléphone'}}) }}
							<div class="input-group-append">
								<span class="input-group-text">
									<span class="fas fa-phone icon-large"></span>
								</span>
							</div>
						</div>
						{{ form_label(form.description, 'Description', {'label_attr': {'class': 'single-line-label'}}) }}
						<div class="input-group mb-3">
							{{ form_errors(form.description) }}
							{{ form_widget(form.description, {'attr': {'class': 'form-control', 'placeholder': 'Description'}}) }}
						</div>
						{{ form_label(form.imageFile, null, {'label_attr': {'class': 'single-line-label'}}) }}
						<div class="input-group mb-3">
							{{ form_errors(form.imageFile) }}
							{{ form_widget(form.imageFile, {'attr': {'class': 'form-control'}}) }}
						<div class="card-footer text-center">
							<button type="submit" class="btn btn-secondary">Valider</button>
						</div>

					</div>
					{{ form_row(form._token) }}
					{{ form_end(form, {render_rest: false}) }}
				</div>
			</div>

			<div class="text-center mt-4 mb-5">
				<p>Vous avez déjà un compte ?
					<a href="{{ path('app_security.login') }}">Cliquez ici</a>
				</p>
			</div>
		</section>
	{% endif %}
	{% if visitor.producers|length > 0  %}
	YOUPIIIIIIIIII
	{% endif %}
	

	{# <section class="content">
												<div class="container p-5 mb-5">
													<div class="card col-lg-12 col-md-8 col-12 mx-auto p-3">
														<table class="table table-striped table-bordered align-middle">
															<thead>
																<tr class="text-center">
																	<th scope="col" class="d-none d-lg-table-cell">Nom</th>
																	<th scope="col" class="d-none d-lg-table-cell">Catégorie</th>
																	<th scope="col" class="d-none d-lg-table-cell">Prix</th>
																	<th scope="col" class="d-none d-lg-table-cell">producer</th>
																	<th scope="col" class="d-none d-lg-table-cell">Image</th>
																	<th scope="col" class="d-none d-lg-table-cell">Créé le</th>
																	<th scope="col" class="d-none d-lg-table-cell">Mis à jour le</th>
																	<th scope="col"  style="width: 10%" class="d-none d-lg-table-cell">Actions</th>
																</tr>
															</thead>
															<tbody>
																{% for produit in produits %}
																	<tr>
																		<td class="d-none d-lg-table-cell text-center">{{ produit.nom }}</td>
																		<td class="d-none d-lg-table-cell text-center">{{ produit.categorie.nom }}</td>
																		<td class="d-none d-lg-table-cell text-center">{{ produit.prix}}
																			€ /
																		{% if produit.categorie.nom == 'Boissons' %}
																			litre TTC
																		{% else %}
																			kg TTC
																		{% endif %}
																		</td>
																		<td class="d-none d-lg-table-cell text-center">{{ produit.producer.nom }}</td>
																		<td class="d-none d-lg-table-cell text-center"><img src="http://127.0.0.1:8000/images/produits/{{ produit.imageName }}" alt="{{ produit.nom }}" class="image"></td>
																		<td class="d-none d-lg-table-cell text-center">{{ produit.createdAt ? produit.createdAt|date('Y-m-d H:i:s') : '' }}</td>
																		<td class="d-none d-lg-table-cell text-center">{{ produit.updatedAt ? produit.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
																		<td class="d-table-cell d-lg-none d-flex flex-column">
																			<div class="mb-2">
																				<strong>Nom:</strong>
																				{{ produit.nom }}</div>
																			<div class="mb-2">
																				<strong>Catégorie:</strong>
																				{{ produit.categorie.nom }}</div>
																			<div class="mb-2">
																				<strong>Prix:</strong>
																				{{ produit.prix }}
																				€ /kg</div>
																			<div class="mb-2">
																				<strong>producer:</strong>
																				{{ produit.producer.nom }}</div>
																			<div class="mb-2">
																				<strong>Image:</strong>
																				<img src="{{ asset('images/produits/' ~ produit.imageName) }}" alt="{{ produit.nom }}" class="image"></div>
																			<div class="mb-2">
																				<strong>Créé le:</strong>
																				{{ produit.createdAt ? produit.createdAt|date('Y-m-d H:i:s') : '' }}</div>
																			<div class="mb-2">
																				<strong>Mis à jour le:</strong>
																				{{ produit.updatedAt ? produit.updatedAt|date('Y-m-d H:i:s') : '' }}</div>
																			<div class="d-flex justify-content-around mb-2">
																				<a href="{{ path('app_producer.produit.edit', {'id': produit.id}) }}" class="btn btn-primary">
																					<i class="bi bi-pencil-square color-light"></i>
																				</a>
																				{{ include('backend/addproducer/producerProductDelete.html.twig') }}
																			</div>
																		</td>
																		<td class="d-none d-lg-table-cell">
																			<div class="d-flex justify-content-around">
																				<a href="{{ path('app_producer.produit.edit', {'id': produit.id}) }}" class="btn btn-primary">
																					<i class="bi bi-pencil-square color-light"></i>
																				</a>
																				{{ include('backend/addproducer/producerProductDelete.html.twig') }}
																			</div>
																		</td>
																	</tr>
																{% endfor %}
															</tbody>
														</table>
														<div class="text-center">
															<a href="{{ path('app_producer.produit.ajouter') }}" class="text-center">
																<button type="submit" class="btn btn-color">Ajouter un produit</button>
															<a>
														</div>
													</div>
												</div>
											</section> #}
{% endblock %}
