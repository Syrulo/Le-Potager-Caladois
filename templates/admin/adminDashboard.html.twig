{% extends './layout/base.html.twig' %}

{% block title %}
        Panneau d'administration
        {{ parent() }}
{% endblock %}

{% block body %}
<div class="container mt-5 p-5">
        <h1 class="text-center">Panneau d'administration</h1>
        <div class="d-flex position-relative p-5">
                <div class="col-5">
                <a href="{{ path('app_admin.categorie') }}"><button type="submit" class="btn btn-color">Gérer les catégories</button><a>
                </div>
                <div class="col-5">
                <a href="{{ path('app_admin.producteur') }}"><button type="submit" class="btn btn-color">Gérer les producteurs</button><a>
                </div>
                <div class="col-5">
                <a href="{{ path('app_admin.produit') }}"><button type="submit" class="btn btn-color">Gérer les produits</button><a>
                </div>
        <div>
</div>
{% endblock %}
