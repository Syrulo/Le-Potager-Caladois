{% extends './layout/base.html.twig' %}

{% block title %}
	{{ produit.nom }}
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="content-header">
		<div class="container mt-3 p-5">
			<div class="d-flex justify-content-between align-items-center">
				<div class="text-left">
					<h1>Produit</h1>
				</div>
				<div class="me-5">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="{{ path('app_home_page') }}">Retour à la liste des produits</a>
						</li>
						<li class="breadcrumb-item active">produit</li>
					</ol>
				</div>
			</div>
		</div>
	</section>
	<div class="container-fluid mt-4">
		<div class="card w-50 mx-auto p-3">
			<h1 class="text-center">{{ produit.nom }}</h1>
            <td><img src="http://127.0.0.1:8000/images/produits/{{ produit.imageName }}" alt="{{ produit.nom }}" class="imgFruit"></td>
			<table class="table table-striped">
				<tbody>
				</tbody>
		</table>
	{% endblock %}