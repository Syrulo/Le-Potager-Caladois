{% extends './layout/base.html.twig' %}

{% block title %}
	Mon profil
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="content-header">
		<div class="container mt-3 p-5">
			<div class="d-flex justify-content-between align-items-center">
				<div class="text-left">
					<h1>Mon profil</h1>
				</div>
				<div class="me-5">
					<ol class="breadcrumb">
						<li class="breadcrumb-item">
							<a href="{{ path('app_home_page') }}">Retour à l'accueil</a>
						</li>
						<li class="breadcrumb-item active">Mon profil</li>
					</ol>
				</div>
			</div>
		</div>
	</section>
	<div class="container mt-3 p-3">
		<div class="card w-75 mx-auto p-3 wrapper">
			<h3 class="text-center">Vos informations personnelles</h3>
			<table class="table table-striped table-bordered align-middle">
				<thead>
					<tr>
						<th scope="col">Nom</th>
						<th scope="col">Prenom</th>
						<th scope="col">Nom</th>
						<th scope="col">Telephone</th>
						<th scope="col">Adresse</th>
						<th scope="col">Ville</th>
						<th scope="col">Code postal</th>
						<th scope="col">Actions</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td>{{ utilisateur.utilisateurDetails.nom }}</td>
						<td>{{ utilisateur.utilisateurDetails.prenom }}</td>
						<td>{{ utilisateur.utilisateurDetails.nom }}</td>
						<td>{{ utilisateur.utilisateurDetails.tel }}</td>
						<td>{{ utilisateur.utilisateurDetails.adresse }}</td>
						<td>{{ utilisateur.utilisateurDetails.ville }}</td>
						<td>{{ utilisateur.utilisateurDetails.codePostal }}</td>
						<td>
							<div class="d-flex justify-content-around">
								<a href="{{ path('profil.edit', {'id': utilisateur.id}) }}" class="btn btn-primary">
									<i class="bi bi-pencil-square color-light"></i>
								</a>
							</div>
						</td>
					</tr>
				</tbody>
			</table>
		</div>
	</div>
{% endblock %}
